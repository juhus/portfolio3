 <%= form_for([@website, @portfolio]) do |f| %>
	<%= f.text_field :title %>
	<%= f.text_field :permalink %>
	<%= f.text_area :content, class:"code", id:"code" %>
	<%= f.check_box :publish %>
	<%= f.submit %>
<% end %>