<%= form_for([@website, Portfolio.new], remote: true) do |f| %>
	<%= f.text_field :title, placeholder:"title" %>
	<%= f.text_field :permalink, placeholder:"permalink" %>
	<br /><%= f.submit %>
<% end %>